# ============================================================================
# Email Configuration (Required only if sending emails)
# ============================================================================

# Gmail SMTP Configuration
# For Gmail: Enable 2-Step Verification and generate App Password
# See docs/EMAIL_SETUP.md for detailed instructions
SMTP_USER=your_email@gmail.com
SMTP_PASSWORD=your_gmail_app_password
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Outlook SMTP Configuration (Alternative to Gmail)
# For Outlook: Enable Two-Factor Authentication and generate App Password
# SMTP_USER=your_email@outlook.com
# SMTP_PASSWORD=your_outlook_app_password
# SMTP_HOST=smtp-mail.outlook.com
# SMTP_PORT=587

# ============================================================================
# PostgreSQL Database Configuration (Optional - can also enter interactively)
# ============================================================================

# PostgreSQL Database URL
# Format: postgresql://username:password@host:port/database
# Example: postgresql://postgres:mypassword@localhost:5432/mydb
# POSTGRESQL_DATABASE_URL=postgresql://username:password@host:5432/database

# Alternative: Generic Database URL (must be PostgreSQL format)
# DATABASE_URL=postgresql://user:pass@localhost:5432/mydb

# ============================================================================
# Notes:
# ============================================================================
# 1. Database URLs are OPTIONAL - you can enter them interactively when running the app
# 2. If database URL is set in .env, the app will ask if you want to use it
# 3. Email configuration is REQUIRED only if you plan to send emails
# 4. Never commit .env file to version control (it contains sensitive information)
# 5. Copy this file to .env and update with your actual values

